import {ICON_SIZE} from '@root/types';

const Marker = {
  REGULAR: {
    iconUrl: `./img/pin.svg`,
    iconSize: [ICON_SIZE.WIDTH, ICON_SIZE.HEIGHT]
  },
  ACTIVE: {
    iconUrl: `./img/pin-active.svg`,
    iconSize: [ICON_SIZE.WIDTH, ICON_SIZE.HEIGHT]
  }
};

export default Marker;
